/**
 * Shared utility functions for the OrgaHB Manager front-end bundles.
 *
 * Add cross-bundle helpers here as they emerge. Nothing in this module
 * may import from React, WP globals, or other shared modules — it must
 * remain dependency-free so any entry point can import it without
 * pulling in a bundle-specific dependency chain.
 */
